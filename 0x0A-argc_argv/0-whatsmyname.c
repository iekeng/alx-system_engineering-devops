#include <stdio.h>
/**
  *main - prints the name of program
  *@argc: argument count
  *@argv: argument vector
  *Return: 0
  */
int main(int __attribute__((unused)) argc, char *argv[])
{
	int i = 0;
	
	printf("%s \n", argv[i]);

	return (0);
}

